import { useState, type FormEvent } from "react";
import { platformApi } from "../../platform/api";
export function AgencyOnboarding({ onComplete }: { onComplete: () => void }) { const [error, setError] = useState(""); async function submit(event: FormEvent<HTMLFormElement>) { event.preventDefault(); const d = new FormData(event.currentTarget); try { await platformApi.registerSignup({ accountType: "agency", password: String(d.get("password")), agencyName: String(d.get("agencyName")) }); onComplete(); } catch (cause) { setError(cause instanceof Error ? cause.message : "Setup could not be saved."); } } return <main className="workspace-auth-shell"><section className="workspace-auth-card"><span className="eyebrow">Agency setup</span><h1>Name your agency.</h1><p>You can invite staff and client approvers from the workspace after setup.</p><form onSubmit={submit}><label>Agency name<input required name="agencyName" /></label><label>Password<input required minLength={12} type="password" name="password" autoComplete="new-password" /></label>{error && <p role="alert">{error}</p>}<button>Finish agency setup</button></form></section></main>; }
