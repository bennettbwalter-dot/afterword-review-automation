import { useState, type FormEvent } from "react";
import { platformApi } from "../../platform/api";
export function BusinessOnboarding({ onComplete }: { onComplete: (result: { businessId?: string; locationId?: string }) => void }) {
  const [error, setError] = useState(""); const [busy, setBusy] = useState(false);
  async function submit(event: FormEvent<HTMLFormElement>) { event.preventDefault(); const d = new FormData(event.currentTarget); setBusy(true); setError(""); try { onComplete(await platformApi.registerSignup({ accountType: "business", password: String(d.get("password")), businessName: String(d.get("businessName")), locationName: String(d.get("locationName")), country: String(d.get("country")) as "GB" | "US", timezone: String(d.get("timezone")) })); } catch (cause) { setError(cause instanceof Error ? cause.message : "Setup could not be saved."); } finally { setBusy(false); } }
  return <main className="workspace-auth-shell"><section className="workspace-auth-card"><span className="eyebrow">Business setup</span><h1>Add your first location.</h1><form onSubmit={submit}><label>Business name<input required name="businessName" /></label><label>Location name<input required name="locationName" /></label><label>Country<select name="country" defaultValue="GB"><option value="GB">United Kingdom</option><option value="US">United States</option></select></label><label>Timezone<input required name="timezone" defaultValue="Europe/London" /></label><label>Password<input required minLength={12} type="password" name="password" autoComplete="new-password" /></label>{error && <p role="alert">{error}</p>}<button disabled={busy}>{busy ? "Saving…" : "Continue to Google"}</button></form></section></main>;
}
